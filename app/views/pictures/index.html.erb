<% provide(:title, @user.name + ' Pictures') %>

<div class="row">
	<div class="col-md-12">
		<% if @user.pictures.any? %>
			<h3>Pictures (<%= @user.pictures.count %>)</h3>
			<ul class="pictures">
				<%= render @pictures %>
			</ol>
			<%= will_paginate @pictures %>
		<% end %>
	</div>
</div>