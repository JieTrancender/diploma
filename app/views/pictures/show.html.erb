<% provide(:title, @user.name) %>

<div class="row">
	<div class="col-md-12">
		<% if @user.pictures.any? %>
			<h3>Pictures (<%= @user.pictures.count %>)</h3>
			<ol class="pictures">
				<%= render @pictures %>
			</ol>
			<%= will_paginate @pictures %>
		<% end %>
	</div>
</div>